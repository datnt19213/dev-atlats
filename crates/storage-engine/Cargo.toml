[package]
name = "devatlas-storage-engine"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
devatlas-common = { path = "../common" }
rusqlite = { version = "0.32", features = ["bundled"] }
serde_json = "1"
